// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.2
//   protoc               v6.33.4
// source: gateway/webhook.proto

/* eslint-disable */
import type { Empty } from "../google/protobuf/empty";

export const protobufPackage = "fitglue.gateway";

/** Strava webhook verification (hub challenge) */
export interface StravaVerificationRequest {
  hubMode: string;
  hubChallenge: string;
  hubVerifyToken: string;
}

export interface StravaVerificationResponse {
  hubChallenge: string;
}

/** Fitbit subscriber verification */
export interface FitbitVerificationRequest {
  verify: string;
}

/** Returns 204 on success — empty body */
export interface FitbitVerificationResponse {
}

/** Generic webhook event payload (Strava/Fitbit POST events) */
export interface WebhookEventRequest {
  payload: Uint8Array;
}

/** Stripe billing webhook */
export interface BillingWebhookRequest {
  /** Stripe-Signature header is read by the handler, not part of the body */
  payload: Uint8Array;
}

/**
 * WebhookGatewayService describes the webhook REST API surface
 * served by api-webhook at /api/webhooks/*.
 *
 * No user authentication — instead uses provider-specific verification
 * (Strava hub challenge, Fitbit subscriber verification, Stripe signatures).
 */
export interface WebhookGatewayService {
  /** ===================== Strava ===================== */
  StravaVerification(request: StravaVerificationRequest): Promise<StravaVerificationResponse>;
  StravaEvent(request: WebhookEventRequest): Promise<Empty>;
  /** ===================== Fitbit ===================== */
  FitbitVerification(request: FitbitVerificationRequest): Promise<FitbitVerificationResponse>;
  FitbitEvent(request: WebhookEventRequest): Promise<Empty>;
  /** ===================== Billing (Stripe) ===================== */
  BillingEvent(request: BillingWebhookRequest): Promise<Empty>;
}
